// This layout provides the shared document structure for public portfolio pages.
function PublicLayout({ children }) {
  return <div className="site-shell">{children}</div>;
}

export default PublicLayout;
